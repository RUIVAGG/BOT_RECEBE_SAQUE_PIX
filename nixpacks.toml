[phases.setup]
nixPkgs = ["nodejs_18", "postgresql"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[deploy]
releaseCommand = "psql $DATABASE_URL -f init.sql"

[start]
cmd = "npm run start"
